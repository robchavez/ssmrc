#' Read text image matrix file
#'
#' Reads a 2D text file and converts it into a matrix. Useful for masking.
#' @param path path string to file 
#' @author Robert S. Chavez
#' @export
#' @examples
#' 
#' read_image_txt('~/my_mask_file.txt')

read_image_txt <- function(path){

  data <- as.matrix(read.table(path))
  return(data)
  
}